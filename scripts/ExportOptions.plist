<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Export options for `xcodebuild -exportArchive`.

  Uses Developer ID distribution (direct download / Homebrew cask), NOT the
  App Store. This requires a "Developer ID Application" certificate, which is
  NOT yet installed on this machine — see scripts/build-release.sh.
-->
<plist version="1.0">
<dict>
    <key>method</key>
    <string>developer-id</string>

    <key>teamID</key>
    <string>Z7S48Q4N3Z</string>

    <!-- Automatic: Xcode picks the installed Developer ID Application cert. -->
    <key>signingStyle</key>
    <string>automatic</string>

    <!-- Hardened Runtime is enabled in the target; keep it on for notarization. -->
    <key>destination</key>
    <string>export</string>
</dict>
</plist>
